name: Run TaxDeed Scraper

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 11 * * *" # 11:00 UTC todo dia

concurrency:
  group: taxdeed-scraper
  cancel-in-progress: true

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install OS deps (Tesseract OCR)
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browsers + OS deps
        run: |
          python -m playwright install --with-deps chromium

      - name: Run scraper
        env:
          HEADLESS: "true"
          
          SKIP_IF_ADDRESS_NOT_NUMBERED: "true" 

          # batching / throttling
          BATCH_SIZE: "100"
          PAUSE_HOURS_ON_DONE: "0"   # não precisa pausar, pq roda 1x/dia

          # change detection
          MARK_REMOVED: "true"

          # identity / scope
          SCRAPER_NAME: "orange_taxdeed"
          COUNTY: "Orange"
          STATE: "FL"

          # App ingest (Vercel)
          APP_API_BASE: ${{ secrets.APP_API_BASE }}
          APP_API_TOKEN: ${{ secrets.APP_API_TOKEN }}

          # State storage (Supabase) - necessário p/ "memória"
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

          STATE_KEY: "orange_taxdeed"
        run: |
          python scraper.py
